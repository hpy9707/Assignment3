struct PixelInput
{
	float4 position : SV_POSITION;
	float4 colour : COLOR;
	float3 normal : NORMAL;
	float2 uv : TEXCOORD;
};

float4 main(PixelInput input) : SV_TARGET
{
	// Saturate is basically a clamp that keeps values between 0 and 1
	return saturate(input.colour);
}